node_modules
.next
out
build
.DS_Store
*.pem
npm-debug.log*
.env*.local
next-env.d.ts
.vercel
*.tsbuildinfo

# Local agent / n8n-as-code tooling & config (regenerated, never commit)
.agents
.n8nac
n8nac-config.json

# n8n-as-code sync state (regenerated on every push)
.n8n-state.json
.n8n-sync-events.jsonl

# Playwright MCP test artifacts
.playwright-mcp
